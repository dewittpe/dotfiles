
if (interactive()) {
  options(repos = c(CRAN = "http://cran.rstudio.com"))
  options(vimcom.verbose = 1)
  options(stringsAsFactors = FALSE)
  library(vimcom)
  library(colorout)
  library(setwidth)
}
