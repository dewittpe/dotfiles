.First <- function() {
  if (interactive()) {
    options(repos = c(CRAN = "https://cran.rstudio.com",
                      BIOC = "https://bioconductor.org/packages/release/bioc/"))
    library(colorout)
  }
}
